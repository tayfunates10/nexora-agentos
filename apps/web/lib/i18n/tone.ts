/**
 * The four presentational tones a state can carry. A tone is an accent on text that
 * already says the state in words: it is never the only thing carrying the meaning, so it
 * stays out of the message dictionaries and never changes with the language.
 */
export type Tone = "up" | "down" | "warn" | "neutral";
