apiVersion: v1
kind: ServiceAccount
metadata:
  name: nexora
  namespace: nexora
  labels:
    app.kubernetes.io/part-of: nexora
# No Nexora workload talks to the Kubernetes API, so no pod receives a token for it.
automountServiceAccountToken: false
